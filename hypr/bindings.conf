# Application bindings
$terminal = uwsm app -- $TERMINAL
$browser = omarchy-launch-browser

# bindd = SUPER, <KEY>, Description, exec, command

bindd = SUPER, return, Terminal, exec, $terminal --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER, E, File manager, exec, uwsm app -- nautilus --new-window
bindd = SUPER, B, Browser, exec, $browser
bindd = SUPER SHIFT, B, Browser (private), exec, $browser --private
bindd = SUPER SHIFT, S, Print, exec, omarchy-cmd-screenshot
bindd = SUPER, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER, T, Activity, exec, $terminal -e btop
# bindd = SUPER, G, Signal, exec, omarchy-launch-or-focus signal "uwsm app -- signal-desktop"
bindd = SUPER, O, Obsidian, exec, omarchy-launch-or-focus obsidian "uwsm app -- obsidian -disable-gpu --enable-wayland-ime"

# If your web app url contains #, type it as ## to prevent hyperland treat it as comments
bindd = SUPER A, G, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"
bindd = SUPER A, C, Claude, exec, omarchy-launch-webapp "https://claude.ai/"

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
unbind = SUPER, SPACE
bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu

unbind = SUPER, W
bindd = SUPER SHIFT, Q, Close active, killactive

unbind = SHIFT, F1
bindd = SUPER, F, Toggle fullscreen, fullscreen
